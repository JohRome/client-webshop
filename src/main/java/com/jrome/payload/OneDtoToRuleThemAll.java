package com.jrome.payload;

public interface OneDtoToRuleThemAll {
    // Name can be changed to something more appropriate if needed...
    // Just a marker interface allowing that gets implemented by a DTO. This way it gets easier to (de)serialize JSON
}
